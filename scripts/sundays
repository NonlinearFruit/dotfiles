#!/usr/bin/env nu

def main [
  birthdate: string # Someone's birthday
] {
  $birthdate
  | date to-record
  | update year {$in + 18}
  | $"($in.month)-($in.day)-($in.year)"
  | into datetime
  | datediff $in --weeks 
  | into float
  | $in / 7
  | math abs
  | math floor
}
